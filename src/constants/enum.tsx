export enum UnitVariant {
  PLAYER = "player",
  ENEMY = "enemy",
  COIN = "coin",
}

export enum TextUsage {
  SCORE = "score",
  LIVES = "lives",
  STATUS = "status",
  TIME = "time",
}

export enum KeyCode {
  UP = "ArrowUp",
  DOWN = "ArrowDown",
  LEFT = "ArrowLeft",
  RIGHT = "ArrowRight",
}
